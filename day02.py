# 1. 숫자형 vs 문자열
# a = 5
# b = "5"
# a와 b를 더하려고 할 때 에러가 나는 이유는?
# a + b 가 작동하려면 어떻게 고쳐야 할까?
a = 5
b = "5"
b = int(b)

print(a + b)

# 2. 리스트에 리스트 추가

a = [1, 2, 3]
a.append([4, 5])
print(a)

# 출력 결과는? [1, 2, 3, [4, 5]]
# 리스트에 4와 5를 따로 추가하고 싶다면 어떻게 해야 할까?
# a.append(4)
# a.append(5)

# 3. 튜플과 리스트의 차이
# 튜플과 리스트는 어떤 점이 다를까?
# 아래 둘의 차이를 한 문장으로 설명해보자.

# a = [1, 2, 3]   # 리스트
# b = (1, 2, 3)   # 튜플
# 리스트는 수정이 가능하지만 튜플은 불가능하다.

# 4. 딕셔너리 추가
person = {"name": "현석", "age": 30}
person["job"] = "개발자"
# 여기에 "job": "개발자" 항목을 추가해서 출력해보자

print(person)

# 5. 집합(set)의 특징
s = set([1, 2, 2, 3])
print(s)
# 결과는? [1, 2, 3]
# set을 쓰는 이유를 간단히 설명해보자
# 중복되는 요소를 제거할 수 있다.
